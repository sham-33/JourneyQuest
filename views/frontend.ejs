<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>JourneyQuest - Track Your Travels</title>
    <link rel="stylesheet" href="/styles/style.css">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
</head>
<body>
    <!-- Navigation -->
    <nav class="navbar">
        <div class="container">
            <div style="display: flex; justify-content: space-between; align-items: center; width: 100%;">
                <a href="/" class="navbar-brand">
                    <i class="fas fa-map-marked-alt"></i> JourneyQuest
                </a>
                
                <div style="display: flex; gap: 1rem; align-items: center;">
                    <% if (typeof user !== 'undefined' && user) { %>
                        <span style="color: white;">Welcome, <%= user.name %>!</span>
                        <a href="/dashboard" class="btn btn-outline">Dashboard</a>
                        <form action="/auth/logout" method="POST" style="display: inline;">
                            <button type="submit" class="btn btn-outline">Logout</button>
                        </form>
                    <% } else { %>
                        <a href="/auth/login" class="btn btn-outline">Login</a>
                        <a href="/auth/register" class="btn btn-primary">Sign Up</a>
                    <% } %>
                </div>
            </div>
        </div>
    </nav>

    <!-- Hero Section -->
    <section class="hero">
        <div class="container">
            <h1><i class="fas fa-globe-americas"></i> Welcome to JourneyQuest</h1>
            <p>Track your travels, preserve your memories, and explore the world one country at a time.</p>
            
            <% if (typeof user !== 'undefined' && user) { %>
                <div style="display: flex; gap: 1rem; justify-content: center; flex-wrap: wrap;">
                    <a href="/dashboard" class="btn btn-primary">
                        <i class="fas fa-map"></i> View My Map
                    </a>
                    <a href="/images" class="btn btn-outline">
                        <i class="fas fa-images"></i> Photo Gallery
                    </a>
                </div>
            <% } else { %>
                <div style="display: flex; gap: 1rem; justify-content: center; flex-wrap: wrap;">
                    <a href="/auth/register" class="btn btn-primary">
                        <i class="fas fa-user-plus"></i> Start Your Journey
                    </a>
                    <a href="/auth/login" class="btn btn-outline">
                        <i class="fas fa-sign-in-alt"></i> Sign In
                    </a>
                </div>
            <% } %>
        </div>
    </section>

    <!-- Features Section -->
    <section class="py-5" style="background: var(--light-color);">
        <div class="container">
            <div class="text-center mb-5">
                <h2>Why Choose JourneyQuest?</h2>
                <p>Simple, secure, and beautiful travel tracking</p>
            </div>
            
            <div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(300px, 1fr)); gap: 2rem;">
                <div class="card text-center">
                    <i class="fas fa-map-marked-alt" style="font-size: 3rem; color: var(--primary-color); margin-bottom: 1rem;"></i>
                    <h3>Interactive Map</h3>
                    <p>Visualize your travels on a beautiful world map and see your progress.</p>
                </div>
                
                <div class="card text-center">
                    <i class="fas fa-camera" style="font-size: 3rem; color: var(--primary-color); margin-bottom: 1rem;"></i>
                    <h3>Photo Memories</h3>
                    <p>Upload and organize your travel photos by country and destination.</p>
                </div>
                
                <div class="card text-center">
                    <i class="fas fa-shield-alt" style="font-size: 3rem; color: var(--primary-color); margin-bottom: 1rem;"></i>
                    <h3>Secure & Private</h3>
                    <p>Your travel data is protected with industry-standard security.</p>
                </div>
            </div>
        </div>
    </section>

    <!-- Footer -->
    <footer style="background: var(--dark-color); color: white; text-align: center; padding: 2rem 0;">
        <div class="container">
            <p>&copy; 2025 JourneyQuest. Track your adventures, preserve your memories.</p>
        </div>
    </footer>
</body>
</html>
